{
  "name": "rynxpense",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "turbo lint",
    "db:push": "supabase db push --workdir supabase 2>/dev/null || supabase db push"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@11.5.2"
}
